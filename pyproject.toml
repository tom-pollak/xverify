[project]
name = "xverify"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Tom Pollak", email = "tompollak1000@gmail.com" }]
requires-python = ">=3.11"
dependencies = [
    "docstring-parser>=0.16",
    "fastcore>=1.7.29",
    "pydantic>=2.10.6",
    "wandb>=0.19.8",
    "xmltodict>=0.14.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "pytest-cov>=6.0.0",
    "pytest>=8.3.5",
]
tools = ["duckduckgo-search>=7.5.1"]
